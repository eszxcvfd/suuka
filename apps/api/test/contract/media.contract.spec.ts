import { describe, expect, it } from 'vitest';

describe('Media contract', () => {
  it('defines upload contract', () => {
    expect(true).toBe(true);
  });

  it('defines media list contract', () => {
    expect(true).toBe(true);
  });
});
