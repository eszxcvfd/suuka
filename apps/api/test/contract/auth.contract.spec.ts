import { describe, expect, it } from 'vitest';

describe('Auth contract', () => {
  it('defines sign-up request contract', () => {
    expect(true).toBe(true);
  });

  it('defines sign-in response contract', () => {
    expect(true).toBe(true);
  });
});
